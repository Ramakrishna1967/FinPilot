/* Backend location. Same-origin by default (local dev / single-service deploy).
   For a split deploy (static frontend on Vercel, API on Render), set this to
   your Render URL, e.g. "https://finpilot-live.onrender.com". No trailing slash. */
window.FINPILOT_API = "";
